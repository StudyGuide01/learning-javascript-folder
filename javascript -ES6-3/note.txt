what is for of loop
what is promises
what is higher order function
what is find method
what is findInded method // search inded 
what is set method // set method store only unique value
set chiled method (add, has)
what is map (hastables) is not map used in reactjs
what is difference normal object and Map method // oreder problem 2.size problem 3.insersion
what is classes in es6
what is contructure in js // constructure  is a blu print
what is inheritance 
what is es6 modules in js // solve order problem 
what is alias in module
what is oop // paradiam = style of writing code
why oop 














/* es 6*/
1. set method
[

const numbers = [1,2,3,4,5,5,6,1,3];
const newNumber = new Set(numbers);
const arr = Array.from(newNumber);
arr.push(10);
console.log(arr);

]

2. map method
[
const person = new Map();
person.set("name","javed");
person.set('age',25);
console.log(person.get("age"));
]

3. to get length of object key and value 
[
const obj = {
  name: "javed khan",
  age: 35
};

console.log(Object.keys(obj).length);
console.log(Object.entries(obj).length);
console.log(Object.values(obj).length);  

]

4. constructure blue print 
[

function Person(name){

this.name = name;
}

const javed = new Person('javed');
const samir = new Person('samir');

javed.name = 'khan'
console.log(samir);
console.log(javed);

]

*5. class example 

[

class Person{
name;

constructor(name){
this.name = name;
}
greet() {
console.log('good morning');
}

}

const obj1 = new Person('javed');
const obj2 = new Person('samir');

const sayGM =obj1.greet();
// console.log(typeof sayGM);
console.log(obj2);

]

*6. inheritance
[

class Person {
	name;
	age;

	constructor(name,age) {
		this.name = name;
		this.age = age;
	}
	greet() {
		console.log('good morning');
	}

};

class GreatPerson extends Person{
attitude = 'cool';

}

const obj3 = new GreatPerson('john',23);
console.log(obj3);

const obj1 = new Person('javed');
const sayGM = obj1.greet();
// console.log(typeof sayGM);


]